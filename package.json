{
  "name": "opencode-tmux-panes",
  "version": "0.1.0",
  "description": "Automatic tmux pane management for opencode subagents",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun && tsc --emitDeclarationOnly",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.0.0"
  },
  "dependencies": {
    "minimatch": "^10.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "bun-types": "^1.1.0",
    "typescript": "^5.0.0",
    "@opencode-ai/plugin": "^1.1.0"
  }
}
